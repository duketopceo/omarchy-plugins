# lukedaduke.fan

Omarchy bar widget: RAM, CPU load, CPU/GPU/NVMe temps, fan RPM, top memory processes, fan presets.

Helpers live in `bin/`. Fan mode buttons call `omarchy-fan-set` on PATH when present; stats still work without it.

```
j / k   move process list
x       kill selected
Esc     close
```
